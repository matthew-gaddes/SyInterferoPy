name: SyInterferoPy3
channels:
  - defaults
  - conda-forge

dependencies:
  - python                            # general
  - matplotlib
  - numpy
  - spyder                                # 
  - scipy
  - geopy                                 # 
  - shapely                               # for srtm-dem-tools
  - pyshp                                 # for srtm-dem-tools
  - netcdf4                               # for srtm-dem-tools
  - pip                                   # for srtm-dem-tools (opening GSHHS coastlines)
  - pip:
    - wget                                # for srtm-dem-tools (downloading SRTM tiles)
